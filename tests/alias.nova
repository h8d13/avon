// keyword aliases via the `__` internal-pragma namespace
__return = ret
__fn     = func
__if     = when

func int add(int a, int b) {
	ret a + b
}

func int main() {
	int x = add(2, 3);
	when x > 4 {
		print("x is " + x)
	}
	ret x
}
